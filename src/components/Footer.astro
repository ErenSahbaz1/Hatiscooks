---
import { site } from '../data/site';

const year = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="container footer-inner">
    <img class="footer-logo" src="/logo.png" alt={site.name} width="700" height="479" loading="lazy" />
    <ul class="footer-links">
      <li><a href={site.instagram} rel="noopener">Instagram</a></li>
      <li><a href="/contact">Contact</a></li>
    </ul>
    <p class="footer-meta">© {year} Hatiscooks</p>
    <p class="footer-meta">Made with <span aria-hidden="true">♥</span><span class="sr-only">love</span> by Eren doesn't(can't)cooks</p>
  </div>
</footer>

<style>
  .site-footer {
    background: var(--color-soft);
    margin-top: var(--space-8);
  }

  .footer-inner {
    padding-block: var(--space-7);
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    text-align: center;
  }

  /* low-res source — keep small so the soft edges don't show */
  .footer-logo {
    width: 140px;
    height: auto;
    margin-inline: auto;
  }

  .footer-links {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: center;
    gap: var(--space-5);
  }

  .footer-links a {
    color: var(--color-text);
    font-weight: 500;
  }

  .footer-meta {
    font-size: 0.9rem;
    color: var(--color-text-muted);
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    overflow: hidden;
    clip-path: inset(50%);
  }
</style>
